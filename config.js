// URL BATTLER v0.3 frontend config
window.URL_BATTLER_CONFIG = {
  // Cloudflare Worker の /scan エンドポイント。
  // 例: "https://url-battler-scan.example.workers.dev/scan"
  scanEndpoint: "",

  // SNS共有画像・投稿文に載せるURL BATTLER本体URL。
  // 公開時に実URLへ変更してください。
  publicAppUrl: "https://example.com/url-battler/"
};
