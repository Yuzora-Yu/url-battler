// URL BATTLER deployment config
// 公開時は Google Cloud で PageSpeed Insights API 用のキーを作り、
// 「API restrictions: PageSpeed Insights API」および
// 「Application restrictions: HTTP referrers（公開ドメイン）」を設定してから入れてください。
// ローカル試験では画面の「自分のAPIキー」を使う方が簡単です。
window.URL_BATTLER_CONFIG = {
  pageSpeedApiKey: ""
};
